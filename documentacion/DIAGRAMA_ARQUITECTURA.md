# 📊 Diagrama de Arquitectura - Pokedex React

## 🏛️ Vista de Capas

```
┌─────────────────────────────────────────────────────────────┐
│                        APP.TSX (Root)                        │
│                     Entry Point Principal                    │
└────────────────────────────┬────────────────────────────────┘
                             │
            ┌────────────────┴────────────────┐
            │                                 │
┌───────────▼──────────┐          ┌──────────▼────────────┐
│   FEATURE: POKEDEX   │          │   FEATURE: BATTLE     │
│ ┌──────────────────┐ │          │ ┌──────────────────┐  │
│ │   Components     │ │          │ │   Components     │  │
│ │   - Pokedex      │ │          │ │   - BattleArena  │  │
│ │   - PokedexMenu  │ │          │ │   - TrainerModal │  │
│ └──────────────────┘ │          │ └──────────────────┘  │
│ ┌──────────────────┐ │          │ ┌──────────────────┐  │
│ │     Hooks        │ │          │ │     Hooks        │  │
│ │   - usePokemon   │ │          │ │   (futuro)       │  │
│ └──────────────────┘ │          │ └──────────────────┘  │
│ ┌──────────────────┐ │          │ ┌──────────────────┐  │
│ │     Styles       │ │          │ │     Styles       │  │
│ │   - pokedex.css  │ │          │ │   - battle.css   │  │
│ └──────────────────┘ │          │ └──────────────────┘  │
└───────────┬──────────┘          └──────────┬────────────┘
            │                                 │
            └────────────┬────────────────────┘
                         │
          ┌──────────────▼──────────────┐
          │      SHARED LAYER           │
          │  (Código Compartido)        │
          ├─────────────────────────────┤
          │ 📦 Models (Types)           │
          │   - pokemon.model.ts        │
          │   - battle.model.ts         │
          │   - storage.model.ts        │
          ├─────────────────────────────┤
          │ 🔧 Services                 │
          │   - pokemon.service.ts      │
          ├─────────────────────────────┤
          │ 🛠️ Utils                    │
          │   - storage.util.ts         │
          ├─────────────────────────────┤
          │ 🎯 Helpers                  │
          │   - pokemon.helpers.ts      │
          ├─────────────────────────────┤
          │ 📋 Constants                │
          │   - pokemon.constants.ts    │
          │   - storage.constants.ts    │
          ├─────────────────────────────┤
          │ 🧩 Components               │
          │   - DPad.tsx                │
          └─────────────┬───────────────┘
                        │
          ┌─────────────▼───────────────┐
          │      CORE LAYER             │
          │  (Configuración Central)    │
          ├─────────────────────────────┤
          │ ⚙️ Config                   │
          │   - app.config.ts           │
          └─────────────────────────────┘
```

## 🔄 Flujo de Datos

```
┌─────────────┐
│    User     │
│ Interaction │
└──────┬──────┘
       │
       ▼
┌──────────────────┐
│   Component      │ ◄──── (React Component)
│   (Feature)      │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│   Custom Hook    │ ◄──── (Business Logic)
│   (usePokemon)   │
└──────┬───────────┘
       │
       ├──────────────┐
       │              │
       ▼              ▼
┌─────────────┐  ┌──────────────┐
│   Service   │  │    Utils     │ ◄──── (Data Layer)
│ (API Calls) │  │  (Storage)   │
└──────┬──────┘  └──────┬───────┘
       │                │
       ▼                ▼
┌────────────────────────────┐
│      External APIs         │
│   - PokeAPI                │
│   - LocalStorage           │
│   - Socket.io Server       │
└────────────────────────────┘
```

## 🗂️ Estructura de Archivos Detallada

```
src/
├── 📱 App.tsx
├── 🎬 main.tsx
│
├── 🎯 features/
│   ├── 📦 pokedex/
│   │   ├── components/
│   │   │   ├── Pokedex.tsx          [Main Component]
│   │   │   ├── PokedexMenu.tsx      [Menu Component]
│   │   │   └── index.ts             [Barrel Export]
│   │   ├── hooks/
│   │   │   ├── usePokemon.ts        [Pokemon Logic]
│   │   │   └── index.ts
│   │   ├── styles/
│   │   │   ├── pokedex.css
│   │   │   └── pokedex-menu.css
│   │   └── index.ts                 [Feature Export]
│   │
│   └── ⚔️ battle/
│       ├── components/
│       │   ├── BattleArena.tsx      [Battle Component]
│       │   ├── TrainerNameModal.tsx [Modal Component]
│       │   └── index.ts
│       ├── styles/
│       │   ├── battle-arena.css
│       │   └── trainer-modal.css
│       └── index.ts
│
├── 🔗 shared/
│   ├── models/
│   │   ├── pokemon.model.ts         [Pokemon Types]
│   │   ├── battle.model.ts          [Battle Types]
│   │   ├── storage.model.ts         [Storage Types]
│   │   └── index.ts
│   ├── services/
│   │   ├── pokemon.service.ts       [API Service]
│   │   └── index.ts
│   ├── utils/
│   │   ├── storage.util.ts          [Storage Utils]
│   │   └── index.ts
│   ├── helpers/
│   │   ├── pokemon.helpers.ts       [Helper Functions]
│   │   └── index.ts
│   ├── constants/
│   │   ├── pokemon.constants.ts     [Pokemon Constants]
│   │   ├── storage.constants.ts     [Storage Constants]
│   │   └── index.ts
│   ├── components/
│   │   ├── DPad.tsx                 [Shared Component]
│   │   └── index.ts
│   └── index.ts                     [Shared Export]
│
└── ⚙️ core/
    └── config/
        ├── app.config.ts            [App Configuration]
        └── index.ts
```

## 🔀 Patrones de Import/Export

### ✅ Patrón Recomendado (Barrel Exports)

```typescript
// ✅ Clean imports desde features
import { Pokedex, usePokemon } from '@/features/pokedex';

// ✅ Clean imports desde shared
import { Pokemon, PokemonService } from '@/shared';

// ✅ Clean imports específicos
import { loadTeamData } from '@/shared/utils';
import { SHINY_RATE } from '@/shared/constants';
```

### ❌ Patrón Evitado

```typescript
// ❌ Imports largos y específicos
import Pokedex from '@/features/pokedex/components/Pokedex';
import { usePokemon } from '@/features/pokedex/hooks/usePokemon';
import { Pokemon } from '@/shared/models/pokemon.model';
```

## 📈 Ventajas de esta Arquitectura

| Aspecto | Ventaja |
|---------|---------|
| 🎯 **Modularidad** | Cada feature es independiente |
| 🔧 **Mantenibilidad** | Cambios localizados |
| 📦 **Reutilización** | Shared layer para código común |
| 🧪 **Testing** | Fácil de testear por capas |
| 👥 **Colaboración** | Equipos pueden trabajar en paralelo |
| 📚 **Escalabilidad** | Fácil agregar nuevos features |

## 🎨 Separación de Responsabilidades

```
┌─────────────────────────────────────────┐
│           PRESENTATION LAYER            │
│         (React Components)              │
│  - UI Logic                             │
│  - User Interactions                    │
│  - Visual Representation                │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│           BUSINESS LOGIC LAYER          │
│         (Custom Hooks)                  │
│  - State Management                     │
│  - Data Processing                      │
│  - Feature Logic                        │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│           DATA ACCESS LAYER             │
│    (Services, Utils, Helpers)           │
│  - API Calls                            │
│  - Data Transformation                  │
│  - Storage Management                   │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│           EXTERNAL LAYER                │
│    (APIs, Database, Storage)            │
│  - PokeAPI                              │
│  - LocalStorage                         │
│  - Socket.io                            │
└─────────────────────────────────────────┘
```

---

**Nota**: Esta arquitectura sigue principios de **Clean Architecture** y **Feature-Sliced Design**.
